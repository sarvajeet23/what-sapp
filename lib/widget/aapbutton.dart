import 'package:flutter/material.dart';

class AapBarButton extends StatelessWidget {
  const AapBarButton({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return IconButton(
        onPressed: () {}, icon: const Icon(Icons.camera_alt_outlined));
  }
}
